{
    "version": 2,
    "builds": [
        {
            "src": "./index.js",
            "use": "@vercel/node",
            "config": {
                "buildCommand": "prisma generate && next build",
                "installCommand": "npm install && npx prisma generate",
                "prisma": "generate"
            }
        }
    ],
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}
